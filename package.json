{
  "name": "beatschain",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "vercel-build": "cd packages/app && npm install && npm run build"
  },
  "dependencies": {
    "next": "^15.3.1"
  }
}