[phases.setup]
nixPkgs = ['nodejs_20', 'npm']

[phases.install]
cmds = ['cd packages/mcp-server && npm install']

[phases.build]
cmds = ['echo "No build step required for MCP server"']

[start]
cmd = 'cd packages/mcp-server && npm start'