[phases.setup]
nixPkgs = ["nodejs-20_x"]

[phases.install]
cmds = ["cd packages/mcp-server && npm install --production --legacy-peer-deps"]

[phases.build]
cmds = ["echo 'No build needed'"]

[start]
cmd = "cd packages/mcp-server && npm start"