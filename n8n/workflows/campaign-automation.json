{
  "name": "Campaign Automation",
  "nodes": [
    {
      "parameters": {
        "httpMethod": "POST",
        "path": "/webhook/campaign-created"
      },
      "id": "campaign-webhook",
      "name": "Campaign Created",
      "type": "n8n-nodes-base.webhook",
      "position": [240, 300]
    },
    {
      "parameters": {
        "url": "={{$env.MCP_SERVER_URL}}/api/campaigns",
        "sendHeaders": true,
        "headerParameters": {
          "parameters": [
            {"name": "Authorization", "value": "Bearer {{$env.ADMIN_API_KEY}}"}
          ]
        },
        "sendBody": true,
        "bodyParameters": {
          "parameters": [
            {"name": "campaignId", "value": "={{$json.id}}"},
            {"name": "type", "value": "={{$json.type}}"},
            {"name": "content", "value": "={{$json.content}}"}
          ]
        }
      },
      "id": "distribute-campaign",
      "name": "Distribute Campaign",
      "type": "n8n-nodes-base.httpRequest",
      "position": [460, 300]
    }
  ],
  "connections": {
    "Campaign Created": {
      "main": [["Distribute Campaign"]]
    }
  },
  "active": true
}