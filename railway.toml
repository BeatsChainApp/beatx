[build]
builder = "nixpacks"
buildCommand = "cd packages/mcp-server && npm install"

[deploy]
startCommand = "cd packages/mcp-server && npm start"
healthcheckPath = "/healthz"
healthcheckTimeout = 300
restartPolicyType = "on_failure"

[env]
NODE_ENV = "production"
PORT = "4000"